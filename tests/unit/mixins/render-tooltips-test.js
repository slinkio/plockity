import Ember from 'ember';
import RenderTooltipsMixin from 'plockity/mixins/render-tooltips';

module('RenderTooltipsMixin');

// Replace this with your real tests.
test('it works', function() {
  var RenderTooltipsObject = Ember.Object.extend(RenderTooltipsMixin);
  var subject = RenderTooltipsObject.create();
  ok(subject);
});
