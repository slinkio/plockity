import regexValidators from 'plockity/utils/regex-validators';

module('regexValidators');

// Replace this with your real tests.
test('it works', function() {
  var result = regexValidators();
  ok(result);
});
