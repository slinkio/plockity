import {
  silentCount
} from '../../../helpers/silent-count';
import { module, test } from 'qunit';

module('SilentCountHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = silentCount(42);
  assert.ok(result);
});
