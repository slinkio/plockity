import Ember from 'ember';
import layout from '../templates/components/pricing-tables-display';

export default Ember.Component.extend({
  layout: layout,
  classNames: [ 'pricing-table', 'row' ]
});
